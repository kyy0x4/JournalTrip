# Workflow

- Prefers requiring login/authentication before allowing data edits or changes on the web app. Confidence: 0.7
- Prefers the web UI to allow direct input of new data fields as soon as the corresponding column exists in the database (keeps web forms in sync with DB schema). Confidence: 0.6
- Verifies behavior by refreshing the page in the browser; data that doesn't persist across refresh is treated as a real bug (save-to-DB issue) to fix. Confidence: 0.6
- Expects the agent to first explore and understand the project structure/codebase (config files, folders, data layer, routing) before working on tasks. Confidence: 0.5
- Uses multiple AI coding tools (Command Code, OpenCode) and wants to reuse a single subscription/credits across them via provider API instead of paying separately. Confidence: 0.8
- Cost-conscious about AI subscriptions: wants exact, verified billing details (e.g., whether a limit is a shared pool across all models vs. per model, rolling usage caps) before making decisions, not rough summaries; also asks for quantitative value conversions (e.g., how many million tokens a $70/month plan buys for a given model). Confidence: 0.7
- When updating scripts that process per-year Google Sheets, wants the update to keep covering all existing sheets/years (e.g., sync 2025 and 2026 in one run via a loop — the KR script's `SHEET_NAMES` iterates both sheets) rather than swapping a single constant and silently dropping coverage of the other sheet. Confidence: 0.7
- Uses GitHub as the remote for the project; issues short commands like "oke push" / "oke push ke github" / "oke gas push" and expects the agent to run the full git workflow (status check, staged commit with clear message, push) end-to-end without further instruction. Confidence: 0.8
- Approves/triggers offered actions with terse informal commands ("gas update", "oke push", "gas pull", "coba dong bantu deploy", "lanjut semua" to green-light finishing all remaining audit items in one batch, picking among proposed options with "kita coba opsi 1,") and expects the agent to just execute the change immediately — including running the full multi-step deployment (CLI check, project link, secrets, deploy, live tests, cleanup) — without asking for further confirmation. Confidence: 0.85
- When a displayed metric, chart, or UI behavior looks wrong (e.g., a trip duration, a valley in a trend line, a shift toggle that rejects Night and snaps back to Day), asks the agent to double-check ("coba di cek" / "masih ada lembah... coba cek") and expects root-cause investigation — verifying against actual source data, the underlying library behavior, or by reproducing in the browser — before fixing, plus re-verification that the reported symptom is truly gone afterward, following up again if the issue persists (e.g., after an avatar fix, reports back "foto nya masih huruf gitu" to say the symptom remains). Confidence: 0.8
- Treats the original source Google Sheet as ground truth for analytics data; when a computed metric contradicts the sheet (e.g., segment C Bakauheni→destination showing 7 minutes for a trip that actually spanned multiple days — "di sheet itu aslinya beda hari loh"), expects the agent to fix the calculation so it reconstructs the real absolute timeline (cross-midnight/multi-day from bare "HH:MM" checkpoints) and matches the sheet, rather than patching the displayed number. Confidence: 0.7
- When a fix/experiment turns out unsatisfying, prefers abandoning it and reverting the working-tree changes back to the original state via git (e.g., "gajadi dah balikin kaya awal aja") rather than continuing to iterate; already-committed/pushed work is kept untouched. Confidence: 0.7
- Checks behavior on the live/deployed site as well as locally; when a bug reproduces only on live and not on the local build, treats it as a sign the deployed version is stale/out of sync with the repo and pulls the latest code to verify (e.g., "gw cek di live nya ngebug makanya gw coba pull") — expects the agent to compare local/repo state against the deployed version and check deploy config/URL when diagnosing. Confidence: 0.65
- When a reported bug appears in a screenshot but the fix is already present in local code, prefers diagnosing why the screenshot is stale (old build, cached page, unrefreshed HMR) over re-applying the fix; communicates the expected fix to the user and instructs them to hard-refresh (e.g., Cmd+Shift+R) before re-checking. Confidence: 0.6
- Wants local backup/scratch directories (e.g., `.commandcode.bak/`) kept out of commits; agent stages only source and config changes, and confirms the backup was excluded. Confidence: 0.7
- Wants commit messages written in Indonesian with a `fix:` (or similar) conventional-commit prefix and `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>` trailer. Confidence: 0.7
- Prefers applying the security lockdown (RLS policies) right away and dealing with the affected external pipeline (e.g., migrating Apps Script off service_role) as a follow-up, rather than blocking the DB hardening on completing the pipeline refactor first ("gw mau jalanin rls policies dlu nih"). Confidence: 0.7
- The Apps Script → Supabase sync scripts ("send data") delete existing rows for a scope first, then re-insert the latest spreadsheet data — NOT incremental insert/update of new rows. The actual production code deletes per area (`?area=eq.<area>` on trips/leadtimes) then re-inserts all rows for that area, so any replacement (e.g., the sheet-sync edge function) must support delete_then_insert with a match filter (e.g., `{ area }`), and the match must also accept array values (→ IN filter) because the out-of-town leadtimes sync deletes by `tanggal=in.(...)&area=in.(...)`; the spreadsheet stays the source of truth. Confidence: 0.85
- Not every sync script uses delete-then-insert: the KPI Training sync (`driver_training_monthly`) upserts with `on_conflict=nik,bulan` + `Prefer: resolution=merge-duplicates` instead of deleting + reinserting, and the old version wrote via `SUPABASE_ANON_KEY` (which RLS now blocks) — so when converting each Apps Script, the agent must detect and preserve that script's own sync operation (delete_then_insert with match vs upsert with onConflict) in the edge-function replacement rather than assuming a uniform pattern. Confidence: 0.7
- After the agent deploys/sets up an integration, prefers to run the manual end-to-end test himself (e.g., "gw coba nih ya," before testing the Apps Script sync) using a concise quick-start guide, then reports back results/errors (e.g., pasting the exact sync failure message) rather than having the agent perform the final client-side test. Confidence: 0.7
- When the agent provides an example/migration file for the user to copy into their own project (e.g., an Apps Script replacement), it must be complete and runnable standalone: every referenced helper function implemented in full, no placeholder comments like "TETAP SAMA seperti sebelumnya" that skip real code. In practice the user copies only the section they think is relevant, so a missing dependency surfaces immediately as a runtime error — "ReferenceError: callSync is not defined", then again "ReferenceError: callSyncBatched is not defined" once the batched variant replaced plain callSync — or a UI that never appears (e.g., pasting `onOpenTenko` instead of the exact `onOpen` name Google Sheets auto-runs); the user's first assumption when the error appears is that the delivered file is missing the function ("blm ditambahin ya di skrip terbaru?"), so before answering the agent should grep the delivered file to verify what's actually in it and then re-itemize which functions/constants must be copied (including trigger hooks), rather than touching code blindly or trusting the paste. Confidence: 0.8
- Executes manual/external steps himself and confirms completion tersely — e.g., running RLS SQL in the Supabase SQL Editor ("oke gw udah jalanin sql leadtimes nih, kita lanjut selanjutnya") or deploying the app to live/Vercel ("udah di deploy cuy, ini udah live sebenernya, terus no 2?"), or confirming all migrated sync scripts are tested with a bare "udah" — expecting the agent to verify the result server-side, fix any remaining root cause, and proceed straight to the next numbered follow-up item (e.g., key rotation) without re-running or re-explaining the manual step. Confidence: 0.8
- Wants periodic comprehensive audits of the web app covering security gaps, flexibility/maintainability, and data-fetch performance ("audit website kita nih dari celah keamanan, fleksibel, dan fetch data atau audit yang lain biar lancar web nya") — not one-off bug fixes; expects a systematic review (XSS/secret exposure, RLS + storage buckets, auth gating on writes, N+1/unpaginated fetches, bundle size, outdated deps) that ends in prioritized findings (critical/important/minor) with concrete fixes so he can act on the critical items first. Confidence: 0.6
- Values frontend performance as a first-class concern: follows up on audit findings about bundle size (e.g., 1.07 MB index chunk, heavy Recharts/jsPDF) and approves optimization work (manualChunks for big libraries, dynamic imports for rarely-used features like PDF export). Confidence: 0.7
- Treats build/lint hygiene as part of a complete fix: when a build reveals tsc errors from unrelated Deno files under `supabase/functions/`, wants them excluded from the React tsconfig scan (add `exclude`) so `npm run lint`/typecheck stays clean — not left as a pre-existing annoyance. Confidence: 0.65
- Prefers avoiding risky big refactors late in a session: when pages are huge (e.g., 1800- and 1500-line files) but working normally, favors deferring full refactors and instead doing safe, incremental extractions (e.g., pulling a repeated `DriverAvatar` into a shared component) — explicitly justifying "refactor is an investment, not a fix" when deciding to stop. Confidence: 0.6
- Before rotating/regenerating a privileged key (e.g., service_role), expects every external consumer — including separate Google Sheets (e.g., the Tenko sheet) — to be migrated and tested first; when the user reveals a leftover consumer mid-process, the agent must halt the rotation and produce a safe replacement for that consumer too (the user pastes the full script so the agent can convert it). Confidence: 0.7
- Wants "safe" replacements of his Apps Script code to preserve the original business logic verbatim (same FILE_CONFIGS mappings, dedup keys, date parsing, fallback defaults) and only swap the insecure part (manual delete/insert via service_role → one edge-function call), so the delivered example is directly copy-pasteable with no behavior change. Confidence: 0.65
- When the agent delivers an example/migration file and offers to commit it, prefers to copy it into his own project first (e.g., paste into Apps Script) and verify it before the repo commit — the commit can wait ("copy dulu" in response to "commit dulu atau copy dulu?"). Confidence: 0.5
- Reports slow-running sync scripts tersely ("running skrip nya jadi lama banget,") and expects the agent to hunt the actual bottleneck — server-side sequential insert chunking vs. client-side spreadsheet reads (`getDataRange().getValues()`) and the 60s `UrlFetchApp` default timeout — then optimize both ends (bigger chunks + bounded parallel inserts in the edge function, `timeout: 290` in `callSync`) and verify the gain with a measured benchmark on dummy data, rather than shrugging it off; he follows up on the result and re-reports when the slow sync still feels slower than the previous approach. Confidence: 0.65
- When a security-hardening migration replaces a working data pipeline (direct service_role REST calls → edge function), expects the replacement to stay on par performance-wise with the old approach; reports the regression with the concrete result and a baseline comparison ("✅ SYNC BERHASIL! Total 22312 data masuk... lama banget wkwk, lebih lama dari pake service role key sebelumnya") and expects the agent to diagnose the new bottleneck (extra network hop, one giant 22K-row JSON payload) and fix it — e.g., splitting large payloads client-side into ~2000-row batches via `callSyncBatched` — rather than accepting the slowdown as the price of security. Confidence: 0.6
- Maintains separate Apps Script files per sync concern (one for trips/leadtimes, one dedicated solely to Tenko, plus one each for KPI Training → `driver_training_monthly` and Report KR → `kr_reports`) and wants them kept apart — "harusnya di pisah jangan satuin, ini khusus tenko" — so when a helper (e.g., `callSync`/`callSyncBatched`) is needed by any one script, the agent must give it as a self-contained addition to that script (constants + helper + `onOpen` + sync function all in that one file), never by suggesting merging everything into a single combined script; each script carries its own copy of shared helpers; the user proactively enumerates the full inventory of scripts still awaiting conversion ("ada 2 lagi, kpi training(training center) sama report kr") and expects the same dedicated standalone, paste-ready file for each. Confidence: 0.9
- The KR Report sync differs from the other spreadsheet syncs: it deletes per date-range (not per area/key) and inserts in batches of 500 rows to stay under Apps Script's execution time limit ("Insert per batch supaya tidak kena 'Maximum execution time'") — so the edge-function `match` filter had to be extended from `eq`/`in` to support range objects (`{ tanggal_date: { gte, lte } }`), and the converted Apps Script batches large payloads client-side (~2000 rows) so the delete-then-insert request runs once and the rest are plain inserts. Confidence: 0.6
er approves tersely ("iya buatin aja file nya") and expects it delivered ready to paste. Confidence: 0.85
- When a reported problem could be local/environment-specific, cross-verifies it himself on a second device or network before accepting the diagnosis — e.g., "gw coba di komputer kantor buat login lewat github juga sama eror" to confirm a login outage is on the vendor's side, not his machine — and reports the confirmation tersely so the agent can move on to the actual root cause (e.g., checking the vendor's status page) rather than re-debugging his setup. Confidence: 0.5
- Evaluates alternative backend platforms by asking about cost first ("kalau firebase gratis apa bayar cuy") — whether a free tier exists is a decision factor; expects answers framed against the stack already in use (e.g., Supabase free tier vs Firebase Spark/Blaze) with migration implications called out, not just standalone feature lists. Confidence: 0.45
